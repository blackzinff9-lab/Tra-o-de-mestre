function tentarSelecionar(x,y){ selecaoCaminhoInfo=null; const layers=[document.getElementById('texto-layer'),livreLayer]; for(const layer of layers){const els=[...layer.querySelectorAll('path,text,circle,rect,ellipse,line,g,image')].reverse(); for(const el of els){try{const bb=el.getBBox(); if(x>=bb.x-10&&x<=bb.x+bb.width+10&&y>=bb.y-10&&y<=bb.y+bb.height+10){selecaoEl=el;_selRealEl=el;_selCurrentScale=1;_selResizing=false;_selRotating=false;_selRotOrigAngle=0;_selRotCurAngle=0;selecaoTransX=0;selecaoTransY=0; const cur=el.getAttribute('transform')||'';const m=cur.match(/translate\(([-\d.]+)[,\s]+([-\d.]+)\)/);if(m){selecaoTransX=parseFloat(m[1]);selecaoTransY=parseFloat(m[2]);} try{const bb2=el.getBBox();_selBBox={x:bb2.x,y:bb2.y,w:bb2.width,h:bb2.height};}catch(_){_selBBox=null;} desenharHandlesSelecao(el);return true;}}catch(e){}}} for(let ci=0;ci<camadas.length;ci++){const cam=camadas[ci];if(!cam||!cam.visivel||!cam.caminhos)continue; for(let pi=0;pi<cam.caminhos.length;pi++){const c=cam.caminhos[pi]; if(!c.pontos.length)continue; const xs=c.pontos.map(p=>p.x),ys=c.pontos.map(p=>p.y); const minX=Math.min(...xs),maxX=Math.max(...xs); const minY=Math.min(...ys),maxY=Math.max(...ys); const margem=20/scale; if(x>=minX-margem&&x<=maxX+margem&&y>=minY-margem&&y<=maxY+margem){selecaoCaminhoInfo={camadaIdx:ci,caminhoIdx:pi}; c._origPontos=c.pontos.map(p=>({...p})); _selCurrentScale=1; const d=buildPathD(c.pontos,c.fechado); const tmpEl=document.createElementNS(NS,'path');tmpEl.setAttribute('d',d);drawLayer.appendChild(tmpEl);selecaoEl=tmpEl;selecaoTransX=0;selecaoTransY=0;desenharHandlesSelecao(tmpEl);tmpEl.remove(); const sl=document.getElementById('selecao-layer');sl.innerHTML='';const rect=document.createElementNS(NS,'rect');rect.setAttribute('x',minX-4/scale);rect.setAttribute('y',minY-4/scale);rect.setAttribute('width',(maxX-minX)+8/scale);rect.setAttribute('height',(maxY-minY)+8/scale);rect.setAttribute('fill','rgba(3,218,198,0.08)');rect.setAttribute('stroke','#03dac6');rect.setAttribute('stroke-width',1.5/scale);rect.setAttribute('stroke-dasharray',`${5/scale} ${3/scale}`);sl.appendChild(rect);selecaoEl=rect;return true;}}} limparSelecao();return false; }

function desenharHandlesSelecao(el,bbOverride){ const sl=document.getElementById('selecao-layer');sl.innerHTML=''; if(el&&el!==sl)_selRealEl=el; try{let bb; if(bbOverride){bb=bbOverride;}else{try{bb=_selRealEl?_selRealEl.getBBox():el.getBBox();}catch(_){return;}} if(!bb||bb.width===0&&bb.height===0)return; const pad=6/scale; const x0=bb.x-pad,y0=bb.y-pad; const x1=bb.x+bb.width+pad,y1=bb.y+bb.height+pad; const cx=(x0+x1)/2,cy=(y0+y1)/2; _selHandlePositions={x0,y0,x1,y1,cx,cy,hr:14/scale}; const bRect=document.createElementNS(NS,'rect');bRect.setAttribute('x',x0);bRect.setAttribute('y',y0);bRect.setAttribute('width',x1-x0);bRect.setAttribute('height',y1-y0);bRect.setAttribute('fill','rgba(3,218,198,0.07)');bRect.setAttribute('stroke','#03dac6');bRect.setAttribute('stroke-width',1.5/scale);bRect.setAttribute('stroke-dasharray',`${5/scale} ${3/scale}`);sl.appendChild(bRect); const move=document.createElementNS(NS,'circle');move.setAttribute('cx',cx);move.setAttribute('cy',cy);move.setAttribute('r',11/scale);move.setAttribute('fill','#03dac6');move.setAttribute('stroke','white');move.setAttribute('stroke-width',2/scale);sl.appendChild(move);const moveTxt=document.createElementNS(NS,'text');moveTxt.setAttribute('x',cx);moveTxt.setAttribute('y',cy);moveTxt.setAttribute('text-anchor','middle');moveTxt.setAttribute('dominant-baseline','middle');moveTxt.setAttribute('fill','#000');moveTxt.setAttribute('font-size',9/scale);moveTxt.setAttribute('pointer-events','none');moveTxt.textContent='✥';sl.appendChild(moveTxt); const del=document.createElementNS(NS,'circle');del.setAttribute('cx',x1+22/scale);del.setAttribute('cy',y0-22/scale);del.setAttribute('r',10/scale);del.setAttribute('fill','#e74c3c');del.setAttribute('stroke','white');del.setAttribute('stroke-width',1.5/scale);del.setAttribute('pointer-events','all');del.addEventListener('touchstart',(e)=>{e.stopPropagation();e.preventDefault();salvarHistorico(); if(selecaoCaminhoInfo){const cam=camadas[selecaoCaminhoInfo.camadaIdx];if(cam)cam.caminhos.splice(selecaoCaminhoInfo.caminhoIdx,1);selecaoCaminhoInfo=null;renderizarTodos();}else if(_selRealEl){_selRealEl.remove();}limparSelecao();_selRealEl=null;_selCurrentScale=1;},{passive:false});sl.appendChild(del);const delTxt=document.createElementNS(NS,'text');delTxt.setAttribute('x',x1+22/scale);delTxt.setAttribute('y',y0-22/scale);delTxt.setAttribute('text-anchor','middle');delTxt.setAttribute('dominant-baseline','middle');delTxt.setAttribute('fill','white');delTxt.setAttribute('font-size',10/scale);delTxt.setAttribute('font-weight','bold');delTxt.setAttribute('pointer-events','none');delTxt.textContent='✕';sl.appendChild(delTxt); const rotHY_canvas=y0-36/scale;const rotLine=document.createElementNS(NS,'line');rotLine.setAttribute('x1',cx);rotLine.setAttribute('y1',y0);rotLine.setAttribute('x2',cx);rotLine.setAttribute('y2',rotHY_canvas);rotLine.setAttribute('stroke','#ff9800');rotLine.setAttribute('stroke-width',1.2/scale);rotLine.setAttribute('stroke-dasharray',`${3/scale} ${2/scale}`);rotLine.setAttribute('pointer-events','none');sl.appendChild(rotLine); _posicionarHandlesHTML(x0,y0,x1,y1,cx,cy);}catch(err){console.warn('handles err',err);} }

function _posicionarHandlesHTML(x0,y0,x1,y1,cx,cy){ const corners={tl:[x0,y0],tr:[x1,y0],bl:[x0,y1],br:[x1,y1]}; for(const [id,[hx,hy]] of Object.entries(corners)){const sc=canvasParaClient(hx,hy);const div=document.getElementById('sel-handle-'+id);if(!div)continue;div.style.left=sc.x+'px';div.style.top=sc.y+'px';div.style.display='block';} const rotCanvasY=y0-36/scale;const scRot=canvasParaClient(cx,rotCanvasY);const divRot=document.getElementById('sel-handle-rot');if(divRot){divRot.style.left=scRot.x+'px';divRot.style.top=scRot.y+'px';divRot.style.display='flex';} _selHandlePositions.rotCanvasCX=cx;_selHandlePositions.rotCanvasCY=cy; }
function _esconderHandlesHTML(){['tl','tr','bl','br'].forEach(id=>{const d=document.getElementById('sel-handle-'+id);if(d)d.style.display='none';});const r=document.getElementById('sel-handle-rot');if(r)r.style.display='none';}

function _aplicarResize(fator){ _selCurrentScale=fator; const {x:bx,y:by,w:bw,h:bh}=_selBBox; if(selecaoCaminhoInfo){const cam=camadas[selecaoCaminhoInfo.camadaIdx];const c=cam.caminhos[selecaoCaminhoInfo.caminhoIdx];const xs0=c._origPontos.map(p=>p.x),ys0=c._origPontos.map(p=>p.y);const ocx=(Math.min(...xs0)+Math.max(...xs0))/2;const ocy=(Math.min(...ys0)+Math.max(...ys0))/2;c.pontos=c._origPontos.map(p=>({...p,x:ocx+(p.x-ocx)*fator,y:ocy+(p.y-ocy)*fator}));renderizarTodos();const xs=c.pontos.map(p=>p.x),ys=c.pontos.map(p=>p.y);const minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys);desenharHandlesSelecao(null,{x:minX,y:minY,width:maxX-minX,height:maxY-minY});}else if(_selRealEl){const cx=bx+bw/2+selecaoTransX;const cy=by+bh/2+selecaoTransY;_selRealEl.setAttribute('transform',`translate(${cx},${cy}) scale(${fator}) translate(${-(bx+bw/2)},${-(by+bh/2)})`);desenharHandlesSelecao(_selRealEl,{x:cx-(bw/2)*fator,y:cy-(bh/2)*fator,width:bw*fator,height:bh*fator});} }
function _aplicarRotacao(angleDeg){ if(selecaoCaminhoInfo){const cam=camadas[selecaoCaminhoInfo.camadaIdx];const c=cam.caminhos[selecaoCaminhoInfo.caminhoIdx];if(!c._origPontos)c._origPontos=c.pontos.map(p=>({...p}));const xs0=c._origPontos.map(p=>p.x),ys0=c._origPontos.map(p=>p.y);const ocx=(Math.min(...xs0)+Math.max(...xs0))/2;const ocy=(Math.min(...ys0)+Math.max(...ys0))/2;const rad=(angleDeg-_selRotOrigAngle)*Math.PI/180;const cos=Math.cos(rad),sin=Math.sin(rad);c.pontos=c._origPontos.map(p=>{const dx=p.x-ocx,dy=p.y-ocy;return{...p,x:ocx+dx*cos-dy*sin,y:ocy+dx*sin+dy*cos};});renderizarTodos();const xs=c.pontos.map(p=>p.x),ys=c.pontos.map(p=>p.y);const minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys);desenharHandlesSelecao(null,{x:minX,y:minY,width:maxX-minX,height:maxY-minY});}else if(_selRealEl&&_selBBox){const cx=_selBBox.x+_selBBox.w/2+selecaoTransX;const cy=_selBBox.y+_selBBox.h/2+selecaoTransY;_selRealEl.setAttribute('transform',`translate(${cx},${cy}) rotate(${angleDeg}) scale(${_selCurrentScale}) translate(${-(_selBBox.x+_selBBox.w/2)},${-(_selBBox.y+_selBBox.h/2)})`);const hw=(_selBBox.w*_selCurrentScale)/2;const hh=(_selBBox.h*_selCurrentScale)/2;desenharHandlesSelecao(_selRealEl,{x:cx-hw,y:cy-hh,width:hw*2,height:hh*2});} }

function limparSelecao(){ selecaoEl=null;_selRealEl=null;_selResizing=false;_selResizeCorner=null;_selBBox=null;_selCurrentScale=1;_selHandlePositions=null;_selRotating=false;_selRotOrigAngle=0;_selRotCurAngle=0;document.getElementById('selecao-layer').innerHTML='';_esconderHandlesHTML();}

// Handles HTML listeners (resize e rotação) — injetados via setTimeout no app.js
// O código original faz isso em window.onload, movemos para app.js.
