// ====== LIVRE ======
function iniciarLivre(x,y){ if(modoLivre)iniciarPincel(x,y); }
function continuarLivre(x,y){ if(modoLivre)continuarPincel(x,y); }
function finalizarLivre(){ if(modoLivre){finalizarPincel(); if(modoEspelho&&pincelGrupoAtual)aplicarEspelho(pincelGrupoAtual);} }

// ====== BORRACHA ======
function renderizarBorracha(){ borrachaLayer.innerHTML=''; if(pontosBorracha.length===0)return; const ns='http://www.w3.org/2000/svg'; const r1=9/scale; if(pontosBorracha.length>=2){const linePath=document.createElementNS(ns,'path');let d=`M ${pontosBorracha[0].x} ${pontosBorracha[0].y}`;pontosBorracha.forEach(p=>d+=` L ${p.x} ${p.y}`);linePath.setAttribute('d',d);linePath.setAttribute('stroke','#ff6600');linePath.setAttribute('stroke-width',2/scale);linePath.setAttribute('fill','none');linePath.setAttribute('stroke-dasharray',`${10/scale},${5/scale}`);linePath.setAttribute('stroke-linecap','round');borrachaLayer.appendChild(linePath);} if(pontosBorracha.length>=1){const txt=document.createElementNS(ns,'text');const pu=pontosBorracha[pontosBorracha.length-1];txt.setAttribute('x',pu.x+14/scale);txt.setAttribute('y',pu.y-10/scale);txt.setAttribute('fill','#ff6600');txt.setAttribute('font-size',11/scale);txt.setAttribute('font-weight','bold');if(pontosBorracha.length===1)txt.textContent='toque p/ marcar fim do rasgo';else txt.textContent=`${pontosBorracha.length} pontos  •  2x = cortar  •  ↩ = desfazer`;borrachaLayer.appendChild(txt);} pontosBorracha.forEach((p,i)=>{const isLast=i===pontosBorracha.length-1;const outer=document.createElementNS(ns,'circle');outer.setAttribute('cx',p.x);outer.setAttribute('cy',p.y);outer.setAttribute('r',r1);outer.setAttribute('fill',isLast?'#ff6600':'white');outer.setAttribute('stroke','#ff6600');outer.setAttribute('stroke-width',2/scale);borrachaLayer.appendChild(outer);const inner=document.createElementNS(ns,'circle');inner.setAttribute('cx',p.x);inner.setAttribute('cy',p.y);inner.setAttribute('r',r1*0.45);inner.setAttribute('fill',isLast?'white':'#ff6600');borrachaLayer.appendChild(inner);}); }

function aplicarBorracha(){ if(pontosBorracha.length<2)return; const pb0=pontosBorracha[0]; const pbN=pontosBorracha[pontosBorracha.length-1]; const brushR=Math.max(parseFloat(document.getElementById('brush-size').value),8)/scale; const lcDx=pbN.x-pb0.x,lcDy=pbN.y-pb0.y; const lcLen=Math.hypot(lcDx,lcDy); if(lcLen<1)return; const lcNx=lcDx/lcLen,lcNy=lcDy/lcLen; const lcPx=-lcNy,lcPy=lcNx; function distPerpLC(p){const dx=p.x-pb0.x,dy=p.y-pb0.y;return dx*lcPx+dy*lcPy;} function distParalLC(p){const dx=p.x-pb0.x,dy=p.y-pb0.y;return dx*lcNx+dy*lcNy;} function lerp(a,b,t){return{x:a.x+(b.x-a.x)*t,y:a.y+(b.y-a.y)*t,tipo:'ancora'};} function projSegmento(p,a,b){const dx=b.x-a.x,dy=b.y-a.y,lenSq=dx*dx+dy*dy;if(lenSq<0.0001)return{t:0,dist:Math.hypot(p.x-a.x,p.y-a.y)};const t=Math.max(0,Math.min(1,((p.x-a.x)*dx+(p.y-a.y)*dy)/lenSq));return{t,dist:Math.hypot(p.x-(a.x+t*dx),p.y-(a.y+t*dy))};} function acharIntersecoes(pts){const hits=[];for(let i=0;i<pts.length-1;i++){const a=pts[i],b=pts[i+1];const dA=distPerpLC(a);const dB=distPerpLC(b);if((dA-brushR)*(dB-brushR)<=0&&Math.abs(dB-dA)>0.0001){const t=(brushR-dA)/(dB-dA);if(t>=0&&t<=1){const pt=lerp(a,b,t);const pParal=distParalLC(pt);if(pParal>=-brushR*2&&pParal<=lcLen+brushR*2)hits.push({segIdx:i,t,x:pt.x,y:pt.y});}} if((dA+brushR)*(dB+brushR)<=0&&Math.abs(dB-dA)>0.0001){const t=(-brushR-dA)/(dB-dA);if(t>=0&&t<=1){const pt=lerp(a,b,t);const pParal=distParalLC(pt);if(pParal>=-brushR*2&&pParal<=lcLen+brushR*2)hits.push({segIdx:i,t,x:pt.x,y:pt.y});}}} hits.sort((a,b)=>a.segIdx!==b.segIdx?a.segIdx-b.segIdx:a.t-b.t);const dedup=[];for(const h of hits){const last=dedup[dedup.length-1];if(!last||last.segIdx!==h.segIdx||Math.abs(last.t-h.t)>0.01)dedup.push(h);} return dedup;} function construir(pts,hits){if(hits.length<2)return null;const hA=hits[0],hB=hits[hits.length-1];const fA=[];for(let i=0;i<=hA.segIdx;i++)fA.push({...pts[i],tipo:pts[i].tipo||'ancora'});const pA={x:hA.x,y:hA.y,tipo:'ancora'};if(Math.hypot(pA.x-fA[fA.length-1].x,pA.y-fA[fA.length-1].y)>1)fA.push(pA); const fD=[{x:hB.x,y:hB.y,tipo:'ancora'}];for(let i=hB.segIdx+1;i<pts.length;i++)fD.push({...pts[i],tipo:pts[i].tipo||'ancora'});if(fD.length>=2&&Math.hypot(fD[0].x-fD[1].x,fD[0].y-fD[1].y)<1)fD.shift(); return {fA,fD};} function distMediaAoTraco(pts){let soma=0,count=0;for(const p of pts){soma+=Math.abs(distPerpLC(p));count++;}return count>0?soma/count:Infinity;} function projDireta(p,pts){let best=null;for(let i=0;i<pts.length-1;i++){const dx=pts[i+1].x-pts[i].x,dy=pts[i+1].y-pts[i].y;const lenSq=dx*dx+dy*dy;if(lenSq<0.0001)continue;const t=Math.max(0,Math.min(1,((p.x-pts[i].x)*dx+(p.y-pts[i].y)*dy)/lenSq));const x=pts[i].x+t*dx,y=pts[i].y+t*dy;const d=Math.hypot(p.x-x,p.y-y);if(!best||d<best.d)best={segIdx:i,t,x,y,d};}return best;} function hitsComFallback(pts){let hits=acharIntersecoes(pts);if(hits.length>=2)return hits;const h0=projDireta(pb0,pts);const hN=projDireta(pbN,pts);if(!h0||!hN)return [];const limiar=80/scale;if(h0.d>limiar||hN.d>limiar)return[];const arr=[h0,hN].sort((a,b)=>a.segIdx!==b.segIdx?a.segIdx-b.segIdx:a.t-b.t);if(arr[0].segIdx===arr[1].segIdx&&Math.abs(arr[0].t-arr[1].t)<0.005)return[];return arr;} const caminhos=getCaminhos(); let melhorCi=-1,melhorHits=null,melhorDist=Infinity; for(let ci=0;ci<caminhos.length;ci++){const pts=caminhos[ci].pontos;if(pts.length<2)continue;const hits=hitsComFallback(pts);if(hits.length<2)continue;const dist=distMediaAoTraco(pts);if(dist<melhorDist){melhorDist=dist;melhorCi=ci;melhorHits=hits;}} if(melhorCi>=0&&melhorHits){const cam=caminhos[melhorCi];const resultado=construir(cam.pontos,melhorHits);if(resultado){const {fA,fD}=resultado;caminhos.splice(melhorCi,1);let ins=melhorCi; if(fA.length>=2)caminhos.splice(ins++,0,{pontos:fA,fechado:false,stroke:cam.stroke,width:cam.width,opacity:cam.opacity,tipo:cam.tipo}); if(fD.length>=2)caminhos.splice(ins++,0,{pontos:fD,fechado:false,stroke:cam.stroke,width:cam.width,opacity:cam.opacity,tipo:cam.tipo});}} let melhorEl=null,melhorHitsL=null,melhorDistL=Infinity; let melhorElGrupo=null; function _processarPathEl(el,grupoRaiz){const d=el.getAttribute('d')||'';const livPts=[];d.trim().split(/(?=[ML])/).forEach(tok=>{const vals=tok.trim().replace(/^[ML]\s*/,'').split(/[\s,]+/);if(vals.length>=2)livPts.push({x:parseFloat(vals[0]),y:parseFloat(vals[1])});});if(livPts.length<2)return;const hits=hitsComFallback(livPts);if(hits.length<2)return;const dist=distMediaAoTraco(livPts);if(dist<melhorDistL){melhorDistL=dist;melhorEl=el;melhorHitsL=hits;melhorEl._livPts=livPts;melhorElGrupo=grupoRaiz||null;}} [...livreLayer.children].forEach(el=>{if(el.tagName==='g'||el.tagName==='G'){let pathPrincipal=null,maxW=-1; [...el.querySelectorAll('path')].forEach(p=>{const w=parseFloat(p.getAttribute('stroke-width')||'0');const d=p.getAttribute('d')||'';const nPts=(d.match(/[ML]/g)||[]).length; if(nPts>maxW){maxW=nPts;pathPrincipal=p;}});if(pathPrincipal)_processarPathEl(pathPrincipal,el);}else if(el.tagName==='path')_processarPathEl(el,null);}); if(melhorEl&&melhorHitsL){const livPts=melhorEl._livPts; if(melhorElGrupo){melhorElGrupo.remove();if(camadas[camadaAtiva])camadas[camadaAtiva].livreHTML=livreLayer.innerHTML;}else{const resultado=construir(livPts,melhorHitsL);if(resultado){const {fA,fD}=resultado;const st=melhorEl.getAttribute('stroke')||'#000';const sw=melhorEl.getAttribute('stroke-width')||'2';const so=melhorEl.getAttribute('stroke-opacity')||'1';const lc=melhorEl.getAttribute('stroke-linecap')||'round';const lj=melhorEl.getAttribute('stroke-linejoin')||'round';const fi=melhorEl.getAttribute('filter')||'';const da=melhorEl.getAttribute('stroke-dasharray')||''; function mkPath(pontos){if(pontos.length<2)return;const np=document.createElementNS(NS,'path');let nd=`M ${pontos[0].x} ${pontos[0].y}`;pontos.slice(1).forEach(p=>nd+=` L ${p.x} ${p.y}`);np.setAttribute('d',nd);np.setAttribute('stroke',st);np.setAttribute('stroke-width',sw);np.setAttribute('stroke-opacity',so);np.setAttribute('stroke-linecap',lc);np.setAttribute('stroke-linejoin',lj);np.setAttribute('fill','none');if(fi)np.setAttribute('filter',fi);if(da)np.setAttribute('stroke-dasharray',da);livreLayer.appendChild(np);} melhorEl.remove(); mkPath(fA);mkPath(fD);}} } caminhoAtivo=-1;pontosPen=[];penLayer.innerHTML=''; renderizarTodos(); pontosBorracha=[];borrachaLayer.innerHTML=''; }

// ====== FORMAS ======
function toggleFormas(){ const ativo=modoFormas; desativarTodos(); if(!ativo){modoFormas=true; document.getElementById('btnFormas').style.background='#aa00ff'; document.getElementById('modal-formas').style.display='flex';} }
function fecharFormas(){ modoFormas=false;document.getElementById('btnFormas').style.background='#333';document.getElementById('modal-formas').style.display='none'; }
function selecionarForma(f){ formaAtual=f; ['circulo','retangulo','triangulo','linha'].forEach(n=>{document.getElementById('fBtn-'+n).style.background=n===f?'#03dac6':'#333';}); }
function criarElementoForma(ns,x1,y1,x2,y2){ let el; if(formaAtual==='circulo'){el=document.createElementNS(ns,'ellipse');el.setAttribute('cx',(x1+x2)/2);el.setAttribute('cy',(y1+y2)/2);el.setAttribute('rx',Math.abs(x2-x1)/2);el.setAttribute('ry',Math.abs(y2-y1)/2);}else if(formaAtual==='retangulo'){el=document.createElementNS(ns,'rect');el.setAttribute('x',Math.min(x1,x2));el.setAttribute('y',Math.min(y1,y2));el.setAttribute('width',Math.abs(x2-x1));el.setAttribute('height',Math.abs(y2-y1));}else if(formaAtual==='triangulo'){el=document.createElementNS(ns,'polygon');el.setAttribute('points',`${(x1+x2)/2},${y1} ${x1},${y2} ${x2},${y2}`);}else if(formaAtual==='linha'){el=document.createElementNS(ns,'line');el.setAttribute('x1',x1);el.setAttribute('y1',y1);el.setAttribute('x2',x2);el.setAttribute('y2',y2);}return el;}
function aplicarEstiloForma(el){const cor=document.getElementById('col-main').value;const tam=document.getElementById('brush-size').value;const op=document.getElementById('brush-opacity').value;el.setAttribute('stroke',cor);el.setAttribute('stroke-width',tam);el.setAttribute('fill','none');el.setAttribute('stroke-opacity',op);el.setAttribute('stroke-linecap','round');}
function atualizarPreviewForma(x2,y2){formaPreview.innerHTML='';if(!formaStart)return;const ns='http://www.w3.org/2000/svg';const el=criarElementoForma(ns,formaStart.x,formaStart.y,x2,y2);if(!el)return;aplicarEstiloForma(el);el.setAttribute('stroke-dasharray','8,4');formaPreview.appendChild(el);}
function desenharForma(x1,y1,x2,y2){formaPreview.innerHTML='';const ns='http://www.w3.org/2000/svg';const el=criarElementoForma(ns,x1,y1,x2,y2);if(!el)return;aplicarEstiloForma(el);salvarHistorico();livreLayer.appendChild(el);if(camadas[camadaAtiva])camadas[camadaAtiva].livreHTML=livreLayer.innerHTML;}
